// 引擎状态
export enum ENGINE_STATUS {
  READY = 'ready',
  RUNNING = 'running',
  STOPPED = 'stopped'
}

// 微应用状态
export enum APP_STATUS {
  UNLOAD = 'unload',
  LOADED = 'loaded',
  ACTIVED = 'actived',
  DEACTIVED = 'deactived',
  DESTROYED = 'destoryed',
  FAIlED = 'failed'
}
